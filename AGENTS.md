# 全局协作原则

个人跨项目默认协作方式，适用于 Codex / Claude Code / Qoder。项目内或子目录更具体的 `AGENTS.md` / `CLAUDE.md` 优先；项目命令、技术栈约束和业务规则留在项目级文件。

## 沟通

- 思考与回答用简体中文；代码、命令、标识符、日志、引用维持原语言。
- 技术解释面向人类伙伴：用大白话 + 具体场景 / 约定的领域模型词汇，示例优于生硬术语；机制名（CAS、幂等、派生）先解释再使用。
- 完成目标后总结关键工作；经历多轮失败后，总结可复用的经验。

## 编码守则

改编自 [andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills)，偏向谨慎；对显而易见的微小改动自行判断，无需机械套用。

### 编码前思考

- 实施前明确假设；有关键不确定性先问。
- 存在多种合理解释时呈现差异，不静默选一种。
- 有更简单方案就指出；对不合理方案提出异议。
- 不清楚就停下列出困惑点，弄清后再动手。

### 简洁优先

- 只写解决问题所需的最少代码：不加未要求的功能、一次性抽象、推测的灵活性、不可能场景的错误处理。
- 200 行能用 50 行清晰实现就重写。自检："资深工程师会觉得过度设计吗？"

### 精准修改

- 只改必须改的，遵循现有代码风格；每一行改动都能追溯到用户请求。
- 清理本次改动产生的孤儿（失效导入/变量/函数）；既有无关死代码只说明、不删。

### 目标驱动

- 把任务转成可验证标准再动手："加校验"→无效输入测试通过；"修 bug"→复现测试通过；"重构 X"→前后测试均绿。
- 多步任务列简短计划，每步写明验证方式。

## 工具与环境

### 搜索与网页

- 联网搜索走 exa MCP：GitHub 代码库优先，其次网页；优先复用已知代码模式。
- 抓取网页内容优先 playwright-cli；环境自带 WebFetch 可用时可直接用。

### 网络

- 境外网站/资源统一走本地代理 `http://127.0.0.1:7890`，失败先检查代理可用性再重试。
- Linux 软件源、DockerHub 等优先用国内镜像（清华、阿里云）。

## Windows 本机坑

跑 bash / gh / cmd 涉及以下场景时先看这里：

- **gh**：未装 jq 且 `gh issue` 无 `--json`——JSON 解析用 `gh --jq` 或 python；建票/接线用 `--parent` / `--blocked-by`；带完结评论的关闭分两步（先 comment 再 close，别用 `||`/`&&` 拼）。
- **bash 反引号**：双引号包裹的命令里内嵌反引号会被静默命令替换——含反引号的正文先落文件，再 `--body-file` / `-F body=@file` 传参。
- **cmd.exe 引号**：单引号+管道会被拆断，带字面引号的参数会原样传给 API 报 400——封装成脚本用 execFileSync 参数数组拼命令。
- **共享文档并发**：改 issue body 等共享内容前先重取最新全文再整体重写，防并发覆盖。
- **Codex apply_patch 报 `Invalid patch`**：见 [notes/codex-apply-patch.md](notes/codex-apply-patch.md)。

## 本库约定

- `skills/` 中开源技能是指向 `repos/` 的 junction，由 `scripts/update-skills.ps1` 统一拉最新；改进上游技能提 PR 到上游仓库，本地定制放 `skills/local/`。
- 生成 spec（to-spec）前：先读 [notes/to-spec-patches.md](notes/to-spec-patches.md) 的两条补丁。
